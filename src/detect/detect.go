package detect

func SimilarityDetect(str string, anotherStr string) float64 {
	// distance -- recursion algorithm
	// dis = distance(str, anotherStr)
	// return 1 - (dis / MaxLen(str, anotherStr)) 
}
